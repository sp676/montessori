import 'package:flutter/material.dart';

class Constants {
  // App Constants
  static const String appName = 'Montessori App';

  // API Constants
  static const String apiUrl = 'https://api.example.com';
  static const int apiTimeout = 10; // in seconds

  // Database Constants
  static const String dbCollectionUsers = 'users';
  static const String dbCollectionChildren = 'children';

  // UI Constants
  static const double defaultPadding = 16.0;
  static const double borderRadius = 8.0;

  // Colors
  static const primaryColor = Color(0xFF123456);
  static const secondaryColor = Color(0xFF789ABC);
  static const accentColor = Color(0xFFDEF123);
}
