class Child {
  final String id;
  final String name;
  final int age;
  // Add any additional properties related to the Child model

  Child({
    required this.id,
    required this.name,
    required this.age,
    // Initialize additional properties here
  });
}
